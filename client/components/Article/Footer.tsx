import styled from 'styled-components';

const ArticleFooter = styled.div`
  font-size: 12px;
  padding: 16px 0 24px;
`;

const Text = styled.div`
  color: #abcdef;
`;

export { ArticleFooter, Text };
